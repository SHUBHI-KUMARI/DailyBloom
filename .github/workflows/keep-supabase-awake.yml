name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 0 */2 * *" # Every 2 days at 00:00 UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping API
        run: |
          curl -fsS https://craftbook.onrender.com/health/db